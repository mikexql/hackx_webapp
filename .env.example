# Example environment variables for local development
# Copy this file to `.env.local` and fill in values.

# S3 bucket where cases are stored. Either set S3_BUCKET or AWS_S3_BUCKET
S3_BUCKET=scan-case-files
# Optional prefix inside the bucket where case folders live (e.g. cases/)
S3_PREFIX=

# AWS region (optional, defaults to us-east-1)
AWS_REGION=ap-southeast-2

# AWS credentials: set these or use a profile / IAM role in production
# For local testing you can set AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Optional: use a named profile from your AWS config
AWS_PROFILE=

# Optional: override bucket env name
AWS_S3_BUCKET=

# Any other app-specific envs can be added here
